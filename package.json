{
  "name": "deepagents-dev-templates-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/template",
    "test": "npm test -w packages/template",
    "dev": "npm run dev -w packages/template",
    "typecheck": "npm run typecheck -w packages/template",
    "graph": "npm run graph -w packages/template"
  },
  "description": "DeepAgents development templates — monorepo for agent template and dev-agent config",
  "license": "MIT"
}
