registry=https://registry.npmmirror.com

# 沙箱 / Windows NuwaClaw 等环境 pnpm 10/11 混用时统一行为（勿依赖 major 默认）：
# - pnpm 11 默认 minimumReleaseAge=1440、verifyDepsBeforeRun=install，易在 exec/run 前卡死
# - 显式关闭后 9/10/11 行为一致；lockfile 已锁定版本可立即安装
minimum-release-age=0
verify-deps-before-run=false
