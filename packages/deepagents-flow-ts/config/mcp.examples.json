{
  "_comment": "可选 MCP 示例（不会被自动加载）。ask-question 已内置于 mcp.default.json；其余 server 可复制到 servers 或经平台 mcpConfigs 登记。",
  "servers": {
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest"],
      "description": "联网浏览/抓取/页面自动化（chrome-devtools）—— 扩展能力"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${WORKSPACE_ROOT}"],
      "description": "受限文件系统读写（如需与 deepagents 内置 filesystem 不同的边界）—— 扩展能力"
    },
    "bash": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-shell"],
      "description": "shell 执行（如偏好经 MCP 而非内置 bash 工具）—— 扩展能力"
    }
  }
}
