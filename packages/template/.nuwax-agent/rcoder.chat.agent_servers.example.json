{
  "agent_servers": {
    "deepagents-template": {
      "type": "custom",
      "command": "node",
      "args": [
        "${INSTALL_ROOT}/dist/bundle.mjs",
        "--config",
        "${INSTALL_ROOT}/config/app-agent.config.json"
      ],
      "env": {
        "OPENAI_MODEL": "${OPENAI_MODEL}",
        "OPENAI_BASE_URL": "${OPENAI_BASE_URL}",
        "OPENAI_API_KEY": "${SECRET_OPENAI_API_KEY}"
      }
    }
  }
}
