"""App tools — AI-editable tool implementations.

Each module in this package defines a ``create_tool`` function that returns
a pydantic-ai ``Tool`` instance, or a ``register_tools`` function that
registers all tools for the given agent.
"""

from __future__ import annotations
