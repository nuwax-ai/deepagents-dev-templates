[project]
name = "deepagents-dev-templates-python"
version = "0.2.10"
description = "DeepAgents Python application agent template — ACP server, tools, skills, config, distribution. Built on pydantic-ai + pydantic-deepagents."
readme = "README.md"
keywords = [
    "deepagents",
    "acp",
    "ai-agent",
    "pydantic-ai",
    "nuwaclaw",
    "template",
    "agent-client-protocol",
    "llm",
    "tool-calling",
    "subagents",
    "mcp",
]
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Nuwax AI", email = "dev@nuwax.ai" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "pydantic-ai>=1.97.0",
    "pydantic-ai-slim[anthropic,openai,web-fetch]>=1.97.0",
    "pydantic-ai-backend[console]>=0.2.9",
    "pydantic-ai-todo>=0.2.3",
    "pydantic-ai-shields>=0.3.3",
    "summarization-pydantic-ai>=0.1.5",
    "subagents-pydantic-ai>=0.2.5",
    "pyyaml>=6.0",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "typer>=0.12",
    "rich>=13.0",
    "prompt-toolkit>=3.0",
    "click>=8.1",
    "anyio>=4.0",
]

[project.optional-dependencies]
yaml = ["pyyaml>=6.0"]
cli = [
    "textual>=3.0.0",
    "typer>=0.12.0",
    "rich>=13.0.0",
    "prompt-toolkit>=3.0.0",
    "python-dotenv>=1.0.0",
]
acp = ["agent-client-protocol>=0.8.0"]
sandbox = ["pydantic-ai-backend[docker]>=0.2.9"]
all = [
    "deepagents-dev-templates-python[yaml,cli,acp,sandbox]",
]

[project.urls]
Homepage = "https://github.com/nuwax-ai/deepagents-dev-templates"
Documentation = "https://github.com/nuwax-ai/deepagents-dev-templates"
Repository = "https://github.com/nuwax-ai/deepagents-dev-templates"
Issues = "https://github.com/nuwax-ai/deepagents-dev-templates/issues"

[project.scripts]
deepagents-app = "deepagents_template.main:main"
deepagents-app-py = "deepagents_template.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/deepagents_template"]

[tool.hatch.build.targets.wheel.shared-data]
"config" = "deepagents_template/config"
"prompts" = "deepagents_template/prompts"
"skills" = "deepagents_template/skills"

[tool.hatch.build.targets.sdist]
include = [
    "src/deepagents_template",
    "config",
    "prompts",
    "skills",
    "docs",
    "scripts",
    "tests",
    ".nuwax-agent",
    "agent-package.json",
    "agent-package.release.json",
    "template.manifest.json",
    "README.md",
    "CLAUDE.md",
    "QUICKSTART.md",
    "LICENSE",
]

[dependency-groups]
dev = [
    "deepagents-dev-templates-python[all]",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "coverage>=7.0",
    "build>=1.0",
    "twine>=5.0",
    "pre-commit>=4.0",
]
lint = [
    "ruff>=0.8",
    "pyright>=1.1",
    "mypy>=1.0",
    "types-PyYAML",
]
docs = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings>=0.26",
    "mkdocstrings-python>=1.12",
]
test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["dist", "build", ".venv", "site"]

[tool.ruff.lint]
select = [
    "E", "F", "I", "UP", "B", "SIM", "Q", "RUF", "C90",
]

[tool.ruff.lint.mccabe]
max-complexity = 15

[tool.ruff.lint.per-file-ignores]
"src/deepagents_template/prompts/**" = ["E501"]
"**/prompts/*.md" = ["E501"]
"tests/**" = ["B008", "E402"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
asyncio_default_fixture_loop_scope = "function"
addopts = ["-ra", "--strict-markers", "--strict-config"]

[tool.coverage.run]
branch = true
source = ["src/deepagents_template"]

[tool.coverage.report]
show_missing = true
skip_covered = true
precision = 2
exclude_lines = [
    "pragma: no cover",
    "pragma: lax no cover",
    "if TYPE_CHECKING:",
    "@overload",
    "@abstractmethod",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "except ImportError:",
    "pass",
    "\\.\\.\\.",
]

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "basic"
include = ["src/deepagents_template"]
exclude = ["**/__pycache__", ".venv*", "site", "tests", "examples", "dist", "build"]
reportMissingTypeStubs = false
reportUnknownMemberType = false
reportUnknownArgumentType = false
reportUnknownVariableType = false
reportUnknownParameterType = false
reportUnusedFunction = false
reportPrivateUsage = false
reportOptionalMemberAccess = false
reportCallIssue = false
reportAssignmentType = false

[tool.mypy]
python_version = "3.11"
strict = false
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false
disable_error_code = ["arg-type", "list-item", "abstract"]
